import { Service } from '../types';
export function ServiceDetail({ service, onBack, onBookService }: { service: Service; onBack:()=>void; onBookService:(name:string)=>void }) {return <section className="py-14 lg:py-20 bg-white"><div className="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8"><button onClick={onBack} className="mb-6 font-bold text-[#1E4ED8]">← Back to Services</button><div className="grid lg:grid-cols-2 gap-10"><img src={service.imageUrl} className="rounded-xl w-full aspect-[4/3] object-cover"/><div><p className="text-[#1E4ED8] font-bold mb-3">{service.category}</p><h1 className="text-3xl lg:text-5xl font-black mb-5">{service.title}</h1><p className="text-slate-600 leading-8 mb-6">{service.longDescription}</p><div className="grid gap-3 mb-8">{service.features.map(f=><div key={f} className="p-4 rounded-xl bg-slate-50 font-semibold text-slate-700">{f}</div>)}</div><p className="font-bold mb-5">{service.priceInfo}</p><button onClick={()=>onBookService(service.title)} className="px-7 py-4 bg-[#0A1628] text-white rounded-full font-bold">Request This Service</button></div></div></div></section>}
